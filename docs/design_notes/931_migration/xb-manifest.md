# Replacement text for `validation/crossval/manifest.json` — group X-B

Owner of this file: the X-B crossval agent (cv06b, cv07, cv11; cv09/cv10 as
controls). `manifest.json` itself is NOT edited by this branch; the merge /
ingest agent applies what is below, after the three VESSL re-solves land.

Runs: cv06b **369367259191**, cv07 **369367259192**, cv11 **369367259194**
(all `rfx-931-post-*`, preset `gpu-rtx4090`, cluster `remilab-c0`).
Their pre-change baselines are 369367259002 / 259003 / 259004.
Per-case detail, fixture key lists and the pre-declarations live in:

* `validation/crossval/_06b_msl_notch_results/RECOMPUTE.md`
* `validation/crossval/_07_sheen_results/RECOMPUTE.md`
* `validation/crossval/_11_wr90_port_results/RECOMPUTE.md`

---

## `cases[06b_msl_notch_filter_uniform]`

**`claim_scope` — what must change.** Every quoted criterion-A and
criterion-B value is regenerated by run 369367259191; take them from
`_06b_msl_notch_results/cv06b_build_falsifiers_summary.json` after the run,
by JSON path as today. In addition, these two SENTENCES have to change
because the realization did, not because a number moved:

1. Wherever the scope says the trace realizes 635.0 µm, it must now
   distinguish the two quantities #931 separated:

   > the metal is declared as two zero-thickness SHEETS on the substrate-top
   > node plane (z = 254 µm); each realizes 10 node rows, i.e. a GEOMETRIC
   > extent of 571.5 µm (the node span `fidelity_report` prints) and an
   > ELECTRICAL width of 635.0 µm (n_rows·dx, what the Hammerstad-Jensen
   > reference takes). The pre-#931 rule reported 635.0 µm as the geometric
   > extent because it zeroed one edge past each footprint's hi rim.

2. The claim "this script's own substrate now realizes h_sub = 254 µm
   exactly" stands unchanged — dielectric sampling is fenced by design note
   §1.8 and the substrate row is bit-identical (verified build-only).

**Do not** re-pin `NOTCH_FREQ_TOL_PCT` (4.0), `STOPBAND_BW_FRAC_IDEAL`
(0.210274) or the BW ratio window (0.80, 1.20): no window moved.

**`gate_paths`** gains the build-time gate, which is new and runs without a
solve:

    tests/crossval/test_msl_notch_public_carriers.py::test_build_time_assertion_accepts_the_shipped_geometry

---

## `cases[07_sheen_lpf]`

**`claim_scope` — what must change.** Every rfx-leg figure it recites
(argmin 7.874 GHz, passband diff 0.0014, 0/120 correction bins, witness
−70.2/−71.3 dB, structure distance 1.52 % vs openEMS 0.66 %,
`sides_with = openems`) is regenerated by run 369367259192. The openEMS-side
figures do not move. Two additions:

1. the declared-vs-realized table the scope leans on changes, because the
   metal is now three SHEETS on one node plane instead of three one-cell
   Boxes that the contract would realize as a 200 µm slab with two walls:

   > realized at dx = 200 µm: sheet plane z = 800 µm (= the realized
   > laminate face; declared foil plane 794 µm, +6.0 µm), both 50-Ω feeds
   > 2200 µm wide over 12 node rows, patch 20200 × 2400 µm. Pre-#931 the
   > same board reported 2400 / 20400 / 2600 µm — one cell more on every
   > in-plane extent, the rim edge the old rule zeroed. The strips'
   > current-carrying row counts are unchanged, so their electrical widths
   > are unchanged.

2. the port mismatch recorded in the script at :73-86 is re-measured and
   still OPEN: each port spans 13 node rows while its feed metal realizes
   12. The scope should say so, and say that closing it needs §1.9 core work
   (the port taking its cross-section from the realized sheet footprint),
   because passing the port the contract's realized extent would set the
   wave split's reference to HJ(2200, 800) = 54.22 Ω on a line this case
   measures at 50.30 Ω.

**Do not widen** C4 (±0.50 %) or C6 (±0.25 %): both derive from the cell
size, which did not change. A doublet moving more than its window, or
`referee_sides_with` flipping, is a finding to write down — not a re-pin.

**`gate_paths`** gains:

    tests/crossval/test_cv07_realized_metal.py

---

## `cases[11_waveguide_port_wr90]`

**`claim_scope` — what must change.** The scope currently recites the
broad-E4 artifact provenance (0.0186 rebuilt vs 0.0707 pinned, 3.6×–3.8×)
and names that artifact as its only `artifact_path`. Run 369367259194
forces the pending refresh decision. When it is taken, BOTH deltas must be
explained in the scope — June→August code change, and August→post-#931
realization change — and never spliced into one number.

New sentences the scope needs:

> The in-script port-aperture trim (`y_range` / `z_range` naming the last
> interior cell column) is DELETED. #889 fixed the rfx-side default
> (`_node_span_to_cell_span`: the transverse mode is solved on the guide's N
> cells), which made the trim the double correction this case predicted it
> would become. Measured build-only: `cfg.f_cutoff` = 6.807677 GHz with the
> trim (+4.454 % against the quote-realized 6.517391 GHz) and 6.512162 GHz
> without it (−0.080 %) — the untrimmed value is exactly what the trim used
> to buy.
>
> The PEC short stays a VOLUME (a 2 mm metal plug, as the Meep and openEMS
> legs draw it) and its realized walls are now asserted before any solve:
> 145.000 / 146.000 / 147.000 mm. The far face at 147 mm is new under the
> contract and sits behind the reflector; the reflection plane the
> round-trip phase reference uses is the first wall, 145.000 mm, unchanged.
>
> QUOTE-REALIZED (`A_WG_REALIZED`, `F_CUTOFF_TE10`) is untouched: it reads
> the realized DOMAIN, whose walls are `BoundarySpec` faces, which design
> note §1.8 fences out of the body-ownership contract.

**Do not tighten** the magnitude gates on the new envelope. The script's own
rule is "tighten after #729 settles, on a re-measured envelope", and #729's
port-aperture default is precisely what just moved.

**`gate_paths`** gains:

    tests/crossval/test_cv11_realized_short.py

---

## `cases[09_half_symmetric_waveguide]` and `cases[10_pmc_cpml_half_symmetric]`

**No change.** Both are boundary-face controls with zero geometry entities,
fenced by §1.8. Both were re-run on the migration branch and pass unchanged:

* cv09: a_eff 22.8600 mm, residual 0.0 µm on every axis of both cavities;
  f_full 8.1958 GHz, f_half 8.1959 GHz; invariant 0.0006 % against the
  0.3556 % window; ALL CHECKS PASSED.
* cv10: uniform and non-uniform paths PASS; G1 peak range 0.008 %; G3
  max|H_tan| on y_lo bit-exact 0.0; G4 image control |R−1| = 0.0033 %.

Optional, and the reason the controls are worth naming: add
`tests/crossval/test_cv09_cv10_body_contract_controls.py` to both cases'
`gate_paths`, so "these two must not move" is machine-checkable rather than
a claim in prose.
